# Serial-Data-Logger-with-Live-Plotting
Python-based real-time serial data logger that reads temperature/humidity from a virtual sensor over a COM port loopback, logs it to CSV, and plots it live with Matplotlib.
